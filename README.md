# A Photos wall server in nodejs 

### 1 配置

- 下载express插件 
  ```shell
  npm install express --save
  ```

- 插件mysql
  ```shell
  npm install mysql --save
  ```
- 插件multer，用于图片上传
  ```shell
  npm install multer --save
  ```
- 插件ejs用于解析html
  ```shell
  npm install ejs --save
  ```
